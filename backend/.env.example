NODE_ENV=development
PORT=3001
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/klarix_v2?schema=public"
REDIS_URL="redis://localhost:6379"
# Generate a unique value: openssl rand -hex 32
JWT_SECRET="replace-with-a-32-character-minimum-secret"
# Exactly 64 hex characters, or base64 encoding of exactly 32 bytes.
META_ENCRYPTION_KEY="replace-with-64-hex-characters"
FRONTEND_URL="http://localhost:5173"
